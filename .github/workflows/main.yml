---
name: CI

on:
  push:
  pull_request:

jobs:
  build:
    name: build 
    runs-on: ubuntu-latest
    container: centos:centos7
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - name: Install build requisites
        run: |
          yum -y install rpmdevtools rpmlint rsync
      - name: Build rpm
        run: |
          chmod 777 rpm/teapot-rpm.sh
          rpm/teapot-rpm.sh
          cd $HOME
          ls
          pwd
      - name: Upload rpm
        uses: actions/upload-artifact@v3
        with:
          name: rpms
          path: |
            /github/home/rpmbuild/RPMS/noarch/teapot-v*-1.el7.noarch.rpm

  test:
    name: test
    runs-on: ubuntu-latest
    container: centos:centos7
    steps:
      - name: Install test requisites
        run: |
          yum -y install unzip
      - name: Install the rpm package
        run: |
          run download Build-CI-pipeline
          unzip rpms.zip
          rpm -i rpms/teapot-v*-1.el7.noarch.rpm --nodeps
          
    
