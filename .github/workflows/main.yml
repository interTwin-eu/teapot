---
name: CI

on:
  push:
  pull_request:

concurrency:
  group: ${{ github.workflow }}

jobs:
  build:
    name: build
    runs-on: ubuntu-latest
    container: centos:centos7
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - name: Install build requisites
        run: |
          yum -y install rpmdevtools rpmlint rsync
      - name: Build rpm
        run: |
          chmod 777 rpm/teapot-rpm.sh
          rpm/teapot-rpm.sh
      - name: Upload rpm
        uses: actions/upload-artifact@v3
        with:
          name: teapot-rpm
          path: /github/home/rpmbuild/RPMS/noarch/teapot-v*-1.el7.noarch.rpm

  test:
    name: test
    needs: build
    runs-on: ubuntu-latest
    container:
      image: dvrbanec/teapot-dependencies:latest
      ports:
        - 8081:8081
        - 4242:4242
        - 32400:32400
        - 32401:32401
        - 32402:32402
        - 32403:32403
        - 32404:32404
    services:
      keycloak:
        image: dvrbanec/keycloak-dev:latest
    steps:
      - uses: actions/download-artifact@v3
        with:
          name: teapot-rpm
      - name: Install the rpm package and self-signed certs
        run: |
          rpm -i teapot-v*-1.el7.noarch.rpm --nodeps
          /usr/share/teapot/self-signed-cert-gen.sh
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - name: Test Teapot
        run: |
          eval "$(oidc-agent)"
          python3 keycloak-setup/keycloak-config.py
          oidc-gen --flow password -f keycloak-setup/client_config.json --op-username test-user1 --op-password secret1  --prompt none --pw-file keycloak_setup/pw-file test-user1
          oidc-gen --flow password -f keycloak-setup/client_config.json --op-username test-user2 --op-password secret2  --prompt none --pw-file keycloak_setup/pw-file test-user2
          cp robot/storage-areas /etc/teapot/
          chmod 777 robot/User-sub_generator.sh
          robot/User-sub_generator.sh
          adduser test-user1
          adduser test-user2
          su -c (mkdir -p /home/test-user1/interTwin) test-user1
          # chown test-user1 /home/test-user1/interTwin
          su -c (mkdir -p /home/test-user1/interTwin_extra) test-user1
          # chown test-user1 /home/test-user1/interTwin_extra
          su -c (mkdir -p /home/test-user2/interTwin) test-user2
          # chown test-user2 /home/test-user2/interTwin
          su -c (mkdir -p /home/test-user2/interTwin_extra) test-user2
          # chown test-user2 /home/test-user2/interTwin_extra
          su -c /usr/share/teapot/run-teapot.sh teapot 
          ps faux | grep teapot > /var/lib/teapot/webdav/test
          # curl -k -v -H "Authorization: Bearer $(oidc-token test-user1)" https://$(curl ifconfig.me)/default_area 
          # robot /__w/teapot/teapot/robot/storm-webdav-tests.robot
      - name: Upload configs
        uses: actions/upload-artifact@v3
        with:
          name: test-config
          path: |
            /var/lib/teapot
            /var/log/teapot
            /usr/share/teapot
            /etc/teapot
            /home/test-user1
  